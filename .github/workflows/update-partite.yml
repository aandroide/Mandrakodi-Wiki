# .github/workflows/update-partite.yml
name: Aggiorna partite.json

on:
  schedule:
    # Esegue ogni giorno alle 06:00 UTC
    - cron: "0 6 * * *"
  workflow_dispatch:  # Consente esecuzione manuale

permissions:
  contents: write

jobs:
  fetch-and-save:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Esegui script di estrazione
        run: python get_partite.py

      - name: Commit e push se modificato
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add partite.json
          git diff --cached --quiet || git commit -m "Aggiorna partite.json [ci skip]"
          git push

