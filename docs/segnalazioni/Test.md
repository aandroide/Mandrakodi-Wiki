<!-- partite.html --> <!DOCTYPE html> <html lang="it"> <head>   <meta charset="UTF-8">   <meta name="viewport" content="width=device-width, initial-scale=1.0">   <title>Partite — Serie A, B, C</title>   <style>     body {       font-family: system-ui, -apple-system, sans-serif;       max-width: 960px;       margin: 2rem auto;       padding: 0 1rem;       background: #f9f9f9;       color: #222;     }     h1 { text-align: center; margin-bottom: 0.2em; }     p.subtitle { text-align: center; color: #666; margin-top: 0; }     .competizione-titolo {       margin-top: 2rem;       padding: 0.5rem 0;       border-bottom: 2px solid #333;       font-size: 1.3rem;     }     table {       width: 100%;       border-collapse: collapse;       margin-top: 0.5rem;       background: #fff;     }     th, td {       padding: 0.6rem 0.8rem;       text-align: left;       border-bottom: 1px solid #e0e0e0;     }     th {       background: #333;       color: #fff;       font-weight: 600;     }     tr:hover { background: #f0f7ff; }     .data-col { white-space: nowrap; width: 140px; }     .casa { font-weight: 600; }     .ospite { color: #444; }     .risultato { font-weight: 700; color: #1a73e8; text-align: center; width: 80px; }     .loading { text-align: center; padding: 3rem; color: #888; }     .errore { text-align: center; padding: 2rem; color: #c00; }   </style> </head> <body>   <h1>Calendario Partite</h1>   <p class="subtitle">Serie A · Serie B · Serie C</p>   <div id="contenuto">     <p class="loading">Caricamento partite...</p>   </div>    <script>     async function caricaPartite() {       const contenitore = document.getElementById("contenuto");       try {         const resp = await fetch("partite.json");         if (!resp.ok) throw new Error(`HTTP ${resp.status}`);         const partite = await resp.json();          // Raggruppa per competizione         const gruppi = {};         for (const p of partite) {           const comp = p.competizione || "Altro";           if (!gruppi[comp]) gruppi[comp] = [];           gruppi[comp].push(p);         }          let html = "";         const ordine = ["Serie A", "Serie B", "Serie C"];         const chiavi = ordine.filter(k => gruppi[k]).concat(           Object.keys(gruppi).filter(k => !ordine.includes(k))         );          for (const comp of chiavi) {           html += `<h2 class="competizione-titolo">${comp}</h2>`;           html += `<table>             <thead><tr>               <th class="data-col">Data / Ora</th>               <th>Casa</th>               <th>Risultato</th>               <th>Ospite</th>             </tr></thead><tbody>`;            for (const p of gruppi[comp]) {             html += `<tr>               <td class="data-col">${p.data} ${p.ora}</td>               <td class="casa">${p.casa}</td>               <td class="risultato">${p.risultato || "—"}</td>               <td class="ospite">${p.ospite}</td>             </tr>`;           }            html += `</tbody></table>`;         }          if (!html) {           html = '<p class="errore">Nessuna partita trovata.</p>';         }          contenitore.innerHTML = html;       } catch (err) {         contenitore.innerHTML = `<p class="errore">Errore nel caricamento: ${err.message}</p>`;         console.error(err);       }     }      caricaPartite();   </script> </body> </html>
